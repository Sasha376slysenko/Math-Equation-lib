[build-system]
requires = ["scikit-build-core>=0.3.3", "pybind11"]
build-backend = "scikit_build_core.build"

[project]
name = "equation_ast"
version = "1.0.0"
description = "Mathematical core and equation AST parser"

[tool.scikit-build]
cmake.version = ">=3.15"
